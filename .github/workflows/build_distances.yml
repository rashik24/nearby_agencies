name: Build Distance Matrix

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pandas numpy

      - name: Run distance script
        run: |
          python compute_distances.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: distance-file
          path: agency_distances.csv
      - name: Commit distance file
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add agency_distances.csv
          git commit -m "Update distance matrix"
          git push
